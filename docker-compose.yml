services:
  bridge-monitor:
    image: ghcr.io/1-felix/vodafone-automation:latest
    container_name: vodafone-bridge-monitor
    restart: unless-stopped
    env_file: .env
    # The container needs access to the router on the LAN
    network_mode: host
